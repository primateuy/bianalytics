<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- La sucursal de objetivos se carga desde la ficha del empleado: es un dato de
         RRHH, no de configuración del tablero. -->
    <record id="view_hr_employee_form_targets" model="ir.ui.view">
        <field name="name">hr.employee.form.targets</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <field name="department_id" position="after">
                <field name="target_warehouse_id"
                       groups="primate_sales_targets.group_target_manager"/>
            </field>
        </field>
    </record>

    <record id="view_hr_employee_tree_targets" model="ir.ui.view">
        <field name="name">hr.employee.tree.targets</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_tree"/>
        <field name="arch" type="xml">
            <field name="department_id" position="after">
                <field name="target_warehouse_id" optional="hide"
                       groups="primate_sales_targets.group_target_manager"/>
            </field>
        </field>
    </record>

</odoo>
